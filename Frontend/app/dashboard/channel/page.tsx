import ChannelAnalysisView from "@/components/dashboard/channel/ChannelAnalysisView";
import PageWindow from "@/components/dashboard/PageWindow";

export default function ChannelPage() {
  return (
    <PageWindow>
      <ChannelAnalysisView />
    </PageWindow>
  );
}