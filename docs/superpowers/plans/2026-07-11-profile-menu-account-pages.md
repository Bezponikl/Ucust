# Profile Menu + Account Pages Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expand the profile dropdown menu (`ProfileMenu.tsx`, top-right avatar) with three new
destinations — Оформление (background picker), Подписка (subscription management), Поддержка
(support) — and rename "Настройки" to "Управление аккаунтом".

**Architecture:** Three new mock-data-only pages under `app/dashboard/{appearance,subscription,support}/`,
each backed by a small `lib/dashboard/*.ts` data file and a `components/dashboard/*/​*Settings.tsx`
component, following the exact pattern already established by `/dashboard/account`
(`AccountSettings.tsx` + `components/dashboard/settings/primitives.tsx`). The background picker
also extends `DashboardProvider` with persisted state and adds one new `BackgroundLayer` component
mounted in `DashboardShell`.

**Tech Stack:** Next.js 16 (App Router) / React 19 / Tailwind v4, no backend, no unit test runner
(only `playwright` is installed, used here for manual browser verification, not automated
assertions). Sharp is available transitively (already used by `scripts/process-logo.mjs`) for a
one-off asset-prep script.

## Global Constraints

- No real backend — all data in these pages is static mock data, matching every other dashboard
  page (`AccountSettings.tsx`, `lib/dashboard/mock.ts`, etc.).
- No unit test framework exists in this repo. Each task's "test" step is: `npx tsc --noEmit` for
  type safety, plus a concrete manual verification in the browser (dev server + exact things to
  click/see). This replaces the skill template's automated test-cycle steps.
- Follow existing UI primitives exactly: `SettingsCard`, `Field`, `TextArea`, `SelectField`,
  `Checkbox`, `Toggle`, `SaveButton` from `components/dashboard/settings/primitives.tsx`. Do not
  invent new primitives.
- Icons: use `@/components/ui/Icon` with names from `lib/icons/solar.ts`. Three names used in this
  plan don't exist yet (`card`, `receipt`, `phone`) — Task 1 adds them via the existing
  `scripts/gen-icons.mjs` pipeline. Never hand-edit `lib/icons/solar.ts` (it's autogenerated).
- Payments are UI-only stubs (ЮKassa hosted-checkout concept) — never add card-number input
  fields anywhere.
- The dev server (`npm run dev`) should be running on `localhost:3000` for all manual verification
  steps in this plan. Start it with `npm run dev` (background) if it isn't already running.

---

### Task 1: Add missing Solar icons (`card`, `receipt`, `phone`)

**Files:**
- Modify: `scripts/gen-icons.mjs`
- Modify (regenerated by script, do not hand-edit): `lib/icons/solar.ts`

**Interfaces:**
- Produces: `IconName` gains `"card" | "receipt" | "phone"`, usable as `<Icon name="card" />` etc.
  in every later task.

- [ ] **Step 1: Add the three mappings to `scripts/gen-icons.mjs`**

Open `scripts/gen-icons.mjs` and add these three lines inside the `MAP` object, right after
the `"mail-check": "letter-opened-linear",` line (last entry):

```js
  "mail-check": "letter-opened-linear",
  card: "card-linear",
  receipt: "bill-list-linear",
  phone: "phone-linear",
};
```

(i.e. the `};` that used to close the object now comes after the three new lines.)

- [ ] **Step 2: Regenerate `lib/icons/solar.ts`**

Run: `cd C:\Claude\UCust && node scripts/gen-icons.mjs`
Expected output: `OK: извлечено 54 иконок → lib/icons/solar.ts` (51 existing + 3 new — if the
count differs, open the console output for a `НЕ НАЙДЕНЫ` error and fix the icon name).

- [ ] **Step 3: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
cd C:\Claude\UCust
git add scripts/gen-icons.mjs lib/icons/solar.ts
git commit -m "feat(icons): add card, receipt, phone icons for account pages"
```

---

### Task 2: Prepare and copy background preset images

**Files:**
- Create: `scripts/process-backgrounds.mjs`
- Create (generated by script): `public/backgrounds/bg-1.jpg` … `public/backgrounds/bg-8.jpg`
- Create: `lib/dashboard/appearance.ts`

**Interfaces:**
- Produces: `BACKGROUND_PRESETS: BackgroundPreset[]` (exported from `lib/dashboard/appearance.ts`),
  each `{ id: string; label: string; src: string }`, `src` pointing at `/backgrounds/bg-N.jpg`.
  Consumed by Task 5 (`AppearanceSettings.tsx`).

- [ ] **Step 1: Write the asset-prep script**

Create `scripts/process-backgrounds.mjs`:

```js
// Пережимает пресеты фонов из C:\Users\Ант\OneDrive\Desktop\Фоны в public/backgrounds
// (единый формат/размер для быстрой загрузки как CSS background-image).
// Запуск: node scripts/process-backgrounds.mjs
import sharp from "sharp";
import path from "node:path";
import { mkdirSync } from "node:fs";

const SRC_DIR = "C:/Users/Ант/OneDrive/Desktop/Фоны";
const OUT_DIR = path.resolve("public/backgrounds");

const SOURCES = [
  { file: "autumn-forest-glows-with-vibrant-multi-colored-leaves-generated-by-ai.jpg", out: "bg-1.jpg" },
  { file: "autumn-forest-acrylic-painting-spooky-mystery-dusk-generated-by-ai.jpg", out: "bg-2.jpg" },
  { file: "futuristic-skyscraper-dominates-city-skyline-as-sun-sets-generated-by-ai.jpg", out: "bg-3.jpg" },
  { file: "mar-bustos-HsEz1XZ1TO8-unsplash.jpg", out: "bg-4.jpg" },
  { file: "Background 1.png", out: "bg-5.jpg" },
  { file: "Background 3.png", out: "bg-6.jpg" },
  { file: "Background 49.png", out: "bg-7.jpg" },
  { file: "Background 44.png", out: "bg-8.jpg" },
];

mkdirSync(OUT_DIR, { recursive: true });

for (const { file, out } of SOURCES) {
  const src = path.join(SRC_DIR, file);
  const dest = path.join(OUT_DIR, out);
  await sharp(src)
    .resize({ width: 1920, height: 1920, fit: "inside", withoutEnlargement: true })
    .jpeg({ quality: 78 })
    .toFile(dest);
  console.log(`OK: ${file} → public/backgrounds/${out}`);
}
```

- [ ] **Step 2: Run it**

Run: `cd C:\Claude\UCust && node scripts/process-backgrounds.mjs`
Expected: 8 lines of `OK: ... → public/backgrounds/bg-N.jpg`, and
`Get-ChildItem public/backgrounds` (PowerShell) or `ls public/backgrounds` (bash) shows exactly
`bg-1.jpg` through `bg-8.jpg`.

- [ ] **Step 3: Create the presets data file**

Create `lib/dashboard/appearance.ts`:

```ts
export interface BackgroundPreset {
  id: string;
  label: string;
  src: string;
}

export const BACKGROUND_PRESETS: BackgroundPreset[] = [
  { id: "bg-1", label: "Осенняя долина", src: "/backgrounds/bg-1.jpg" },
  { id: "bg-2", label: "Лесная тропа", src: "/backgrounds/bg-2.jpg" },
  { id: "bg-3", label: "Стеклянный небоскрёб", src: "/backgrounds/bg-3.jpg" },
  { id: "bg-4", label: "Горный фьорд", src: "/backgrounds/bg-4.jpg" },
  { id: "bg-5", label: "Пастельный рассвет", src: "/backgrounds/bg-5.jpg" },
  { id: "bg-6", label: "Мраморные волны", src: "/backgrounds/bg-6.jpg" },
  { id: "bg-7", label: "Шёлковые волны", src: "/backgrounds/bg-7.jpg" },
  { id: "bg-8", label: "Розовые дюны", src: "/backgrounds/bg-8.jpg" },
];
```

- [ ] **Step 4: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
cd C:\Claude\UCust
git add scripts/process-backgrounds.mjs public/backgrounds lib/dashboard/appearance.ts
git commit -m "feat(appearance): add background preset assets and preset data"
```

---

### Task 3: Add persisted `background` state to `DashboardProvider`

**Files:**
- Modify: `components/dashboard/DashboardProvider.tsx`

**Interfaces:**
- Consumes: nothing new (existing `loadOnboarding`, `getDashboardData`).
- Produces: `useDashboard()` now also returns `background: string | null` and
  `setBackground: (v: string | null) => void`. Consumed by Task 4 (`BackgroundLayer`) and
  Task 5 (`AppearanceSettings`).

- [ ] **Step 1: Replace the file contents**

Replace the full contents of `components/dashboard/DashboardProvider.tsx` with:

```tsx
"use client";

import { createContext, useContext, useEffect, useState, type ReactNode } from "react";
import type { DashboardData } from "@/lib/dashboard/types";
import { getDashboardData } from "@/lib/dashboard/mock";
import { loadOnboarding } from "@/lib/onboarding/storage";

const BG_STORAGE_KEY = "uc_bg";

interface Ctx {
  data: DashboardData | null;
  hydrated: boolean;
  /** Скрывает топбар и мобильную нижнюю навигацию (полноэкранные страницы вроде открытого чата) */
  mobileChromeHidden: boolean;
  setMobileChromeHidden: (hidden: boolean) => void;
  /** CSS background-image значение (путь до пресета или data URL) — null = стандартный фон */
  background: string | null;
  setBackground: (v: string | null) => void;
}

const DashboardContext = createContext<Ctx | null>(null);

export function DashboardProvider({ children }: { children: ReactNode }) {
  const [data, setData] = useState<DashboardData | null>(null);
  const [hydrated, setHydrated] = useState(false);
  const [mobileChromeHidden, setMobileChromeHidden] = useState(false);
  const [background, setBackgroundState] = useState<string | null>(null);

  // Профиль доступен только в sessionStorage (клиент) — собираем данные после монтирования.
  useEffect(() => {
    const saved = loadOnboarding();
    /* eslint-disable react-hooks/set-state-in-effect */
    setData(getDashboardData(saved?.profile ?? null));
    setHydrated(true);
    try {
      const savedBg = localStorage.getItem(BG_STORAGE_KEY);
      if (savedBg) setBackgroundState(savedBg);
    } catch {}
    /* eslint-enable react-hooks/set-state-in-effect */
  }, []);

  const setBackground = (v: string | null) => {
    setBackgroundState(v);
    try {
      if (v) localStorage.setItem(BG_STORAGE_KEY, v);
      else localStorage.removeItem(BG_STORAGE_KEY);
    } catch {}
  };

  return (
    <DashboardContext.Provider
      value={{ data, hydrated, mobileChromeHidden, setMobileChromeHidden, background, setBackground }}
    >
      {children}
    </DashboardContext.Provider>
  );
}

export function useDashboard(): Ctx {
  const ctx = useContext(DashboardContext);
  if (!ctx) throw new Error("useDashboard must be used within DashboardProvider");
  return ctx;
}
```

- [ ] **Step 2: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors (no consumer references `background` yet, so this is purely additive).

- [ ] **Step 3: Commit**

```bash
cd C:\Claude\UCust
git add components/dashboard/DashboardProvider.tsx
git commit -m "feat(appearance): add persisted background state to DashboardProvider"
```

---

### Task 4: `BackgroundLayer` component + mount in `DashboardShell`

**Files:**
- Create: `components/dashboard/BackgroundLayer.tsx`
- Modify: `components/dashboard/DashboardShell.tsx`

**Interfaces:**
- Consumes: `useDashboard().background` (from Task 3).
- Produces: nothing consumed by later tasks — this is a leaf rendering component.

- [ ] **Step 1: Create `BackgroundLayer.tsx`**

```tsx
"use client";

import { useDashboard } from "./DashboardProvider";

/** Полноэкранный фоновый слой позади сайдбара/топбара/контента (см. /dashboard/appearance). */
export default function BackgroundLayer() {
  const { background } = useDashboard();
  if (!background) return null;

  return (
    <div className="pointer-events-none fixed inset-0 -z-10" aria-hidden="true">
      <div className="absolute inset-0 bg-cover bg-center" style={{ backgroundImage: `url(${background})` }} />
      {/* Затемняющий scrim для читаемости текста поверх фото — сильнее в тёмной теме */}
      <div className="absolute inset-0 bg-white/25 dark:bg-black/45" />
    </div>
  );
}
```

- [ ] **Step 2: Mount it in `DashboardShell.tsx`**

In `components/dashboard/DashboardShell.tsx`, add the import and render it as the first child
of the root div:

```tsx
import type { ReactNode } from "react";
import DashboardTopBar from "./DashboardTopBar";
import DashboardSidebar from "./DashboardSidebar";
import DashboardBottomNav from "./DashboardBottomNav";
import BackgroundLayer from "./BackgroundLayer";
import SetupTracker from "./SetupTracker";
import Toaster from "@/components/ui/Toaster";

export default function DashboardShell({ children }: { children: ReactNode }) {
  return (
    <div className="flex h-dvh overflow-hidden bg-canvas">
      <BackgroundLayer />
      {/* Сайдбар — только десктоп, мобиле — bottom nav */}
      <DashboardSidebar />

      {/* Правая колонка: тонкий топбар + прокручиваемый контент */}
      <div className="flex min-w-0 flex-1 flex-col overflow-hidden">
        <DashboardTopBar />
        <main className="flex-1 overflow-y-auto px-6 py-5 pb-24 sm:px-8 lg:pb-6">
          <div className="mx-auto w-full max-w-[1280px]">
            {children}
          </div>
        </main>
      </div>

      <DashboardBottomNav />
      <SetupTracker />
      <Toaster />
    </div>
  );
}
```

- [ ] **Step 3: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Manual verification (no visible change yet)**

With `npm run dev` running, open `http://localhost:3000/dashboard` in a browser. The page must
look exactly as before (no background is set yet, so `BackgroundLayer` returns `null`). This
confirms the mount doesn't break the existing shell layout.

- [ ] **Step 5: Commit**

```bash
cd C:\Claude\UCust
git add components/dashboard/BackgroundLayer.tsx components/dashboard/DashboardShell.tsx
git commit -m "feat(appearance): mount BackgroundLayer behind dashboard shell"
```

---

### Task 5: Appearance page (background picker)

**Files:**
- Create: `components/dashboard/appearance/AppearanceSettings.tsx`
- Create: `app/dashboard/appearance/page.tsx`

**Interfaces:**
- Consumes: `BACKGROUND_PRESETS` (Task 2), `useDashboard().background` / `setBackground` (Task 3),
  `SettingsCard` (existing `components/dashboard/settings/primitives.tsx`), `toast` (existing
  `lib/toast.ts`).
- Produces: nothing consumed by later tasks.

- [ ] **Step 1: Create `AppearanceSettings.tsx`**

```tsx
"use client";

import { useRef, useState, type ChangeEvent, type ReactNode } from "react";
import Image from "next/image";
import Icon from "@/components/ui/Icon";
import { SettingsCard } from "@/components/dashboard/settings/primitives";
import { useDashboard } from "@/components/dashboard/DashboardProvider";
import { BACKGROUND_PRESETS } from "@/lib/dashboard/appearance";
import { toast } from "@/lib/toast";

const MAX_UPLOAD_BYTES = 4 * 1024 * 1024; // 4 МБ

function Tile({
  selected,
  onClick,
  label,
  children,
}: {
  selected: boolean;
  onClick: () => void;
  label: string;
  children: ReactNode;
}) {
  return (
    <button
      type="button"
      onClick={onClick}
      aria-pressed={selected}
      className={`group relative flex aspect-video w-full overflow-hidden rounded-2xl border-2 transition ${
        selected ? "border-brand" : "border-border hover:border-brand/40"
      }`}
    >
      {children}
      {selected && (
        <span className="absolute right-2 top-2 flex h-6 w-6 items-center justify-center rounded-full bg-brand text-white shadow">
          <Icon name="check" size={14} aria-hidden="true" />
        </span>
      )}
      <span className="absolute inset-x-0 bottom-0 bg-black/45 px-2 py-1.5 text-left text-xs font-medium text-white">
        {label}
      </span>
    </button>
  );
}

export default function AppearanceSettings() {
  const { background, setBackground } = useDashboard();
  const [customSrc, setCustomSrc] = useState<string | null>(null);
  const fileInput = useRef<HTMLInputElement>(null);

  const onUpload = (e: ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    e.target.value = "";
    if (!file) return;
    if (file.size > MAX_UPLOAD_BYTES) {
      toast("Файл слишком большой, максимум 4 МБ");
      return;
    }
    const reader = new FileReader();
    reader.onload = () => {
      const dataUrl = reader.result as string;
      setCustomSrc(dataUrl);
      setBackground(dataUrl);
    };
    reader.readAsDataURL(file);
  };

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h1 className="text-xl font-bold text-ink sm:text-2xl">Оформление</h1>
        <p className="mt-0.5 text-sm text-ink-muted">Фон дашборда — пресеты или своё изображение</p>
      </div>

      <SettingsCard title="Фон">
        <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">
          <Tile selected={background === null} onClick={() => setBackground(null)} label="Без фона">
            <div className="h-full w-full bg-canvas" />
          </Tile>

          {BACKGROUND_PRESETS.map((p) => (
            <Tile key={p.id} selected={background === p.src} onClick={() => setBackground(p.src)} label={p.label}>
              <Image src={p.src} alt="" fill sizes="200px" className="object-cover" />
            </Tile>
          ))}

          {customSrc && (
            <Tile selected={background === customSrc} onClick={() => setBackground(customSrc)} label="Свой фон">
              <Image src={customSrc} alt="" fill unoptimized className="object-cover" />
            </Tile>
          )}

          <button
            type="button"
            onClick={() => fileInput.current?.click()}
            className="flex aspect-video w-full flex-col items-center justify-center gap-1.5 rounded-2xl border-2 border-dashed border-border text-ink-muted transition hover:border-brand/40 hover:text-ink"
          >
            <Icon name="image-plus" size={22} aria-hidden="true" />
            <span className="text-xs font-medium">{customSrc ? "Заменить свой фон" : "Добавить свой"}</span>
          </button>
          <input ref={fileInput} type="file" accept="image/*" hidden onChange={onUpload} />
        </div>
      </SettingsCard>
    </div>
  );
}
```

- [ ] **Step 2: Create the page route**

Create `app/dashboard/appearance/page.tsx`:

```tsx
import AppearanceSettings from "@/components/dashboard/appearance/AppearanceSettings";

export default function Page() {
  return <AppearanceSettings />;
}
```

- [ ] **Step 3: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Manual verification**

With `npm run dev` running, navigate to `http://localhost:3000/dashboard/appearance`. Verify:
- 10 tiles render: "Без фона" (selected by default, ring visible), 8 presets with thumbnails,
  "Добавить свой" dashed tile.
- Clicking a preset tile immediately changes the real page background behind the sidebar/content
  (visible through the gutters, and through the semi-transparent sidebar as a blur effect) and
  moves the selection ring to that tile.
- Clicking "Без фона" removes the background again.
- Reload the page (`F5`) — the previously selected background persists (localStorage).
- Toggle dark/light theme (via the theme toggle) while a background is set — confirm the scrim
  darkens in dark mode and lightens in light mode, text stays readable.
- Click "Добавить свой", pick any local image under 4 MB — a new "Свой фон" tile appears with a
  thumbnail and becomes selected; the background updates to that image. Try a file over 4 MB (or
  temporarily lower `MAX_UPLOAD_BYTES` to test) — a toast "Файл слишком большой, максимум 4 МБ"
  appears and nothing changes.

- [ ] **Step 5: Commit**

```bash
cd C:\Claude\UCust
git add components/dashboard/appearance app/dashboard/appearance
git commit -m "feat(appearance): add background picker page"
```

---

### Task 6: Subscription mock data

**Files:**
- Create: `lib/dashboard/subscription.ts`

**Interfaces:**
- Produces: `TARIFF_PLANS: TariffPlan[]`, `CURRENT_SUBSCRIPTION: CurrentSubscription`,
  `PAYMENT_HISTORY: PaymentRecord[]`. Consumed by Task 7 (`SubscriptionSettings.tsx`).

- [ ] **Step 1: Create the file**

```ts
export interface TariffPlan {
  id: string;
  name: string;
  price: number;
  tagline: string;
  features: string[];
}

export const TARIFF_PLANS: TariffPlan[] = [
  {
    id: "start",
    name: "Старт",
    price: 1500,
    tagline: "Для одного бизнеса.",
    features: [
      "1 бизнес",
      "До 12 постов в месяц",
      "Все соцсети без ограничений",
      "Генерация фото",
      "Посты по контент-плану",
      "Базовая аналитика",
      "Email-поддержка",
    ],
  },
  {
    id: "business",
    name: "Бизнес",
    price: 3500,
    tagline: "Для регулярного ведения.",
    features: [
      "3 бизнеса",
      "До 30 постов в месяц",
      "Все соцсети без ограничений",
      "Генерация фото и видео",
      "Генерация постов по запросу",
      "Расширенная аналитика",
      "Приоритетная поддержка",
    ],
  },
];

export interface CurrentSubscription {
  planId: string;
  renewsOn: string;
  status: "active" | "cancelled";
}

export const CURRENT_SUBSCRIPTION: CurrentSubscription = {
  planId: "business",
  renewsOn: "11 августа 2026",
  status: "active",
};

export interface PaymentRecord {
  id: string;
  date: string;
  plan: string;
  amount: number;
  status: "Оплачено" | "Отменено";
}

export const PAYMENT_HISTORY: PaymentRecord[] = [
  { id: "p1", date: "11 июля 2026", plan: "Бизнес", amount: 3500, status: "Оплачено" },
  { id: "p2", date: "11 июня 2026", plan: "Бизнес", amount: 3500, status: "Оплачено" },
  { id: "p3", date: "11 мая 2026", plan: "Старт", amount: 1500, status: "Оплачено" },
];
```

- [ ] **Step 2: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
cd C:\Claude\UCust
git add lib/dashboard/subscription.ts
git commit -m "feat(subscription): add mock tariff/payment data"
```

---

### Task 7: Subscription page

**Files:**
- Create: `components/dashboard/subscription/SubscriptionSettings.tsx`
- Create: `app/dashboard/subscription/page.tsx`

**Interfaces:**
- Consumes: `TARIFF_PLANS`, `CURRENT_SUBSCRIPTION`, `PAYMENT_HISTORY` (Task 6), `SettingsCard`
  (existing), `toast` (existing), `TariffConfigurator` (existing component,
  `{ open: boolean; onClose: () => void; onStart: () => void }` props, path
  `@/components/TariffConfigurator`), `card` and `receipt` icons (Task 1).
- Produces: nothing consumed by later tasks.

- [ ] **Step 1: Create `SubscriptionSettings.tsx`**

```tsx
"use client";

import { useState } from "react";
import Icon from "@/components/ui/Icon";
import { SettingsCard } from "@/components/dashboard/settings/primitives";
import TariffConfigurator from "@/components/TariffConfigurator";
import { TARIFF_PLANS, CURRENT_SUBSCRIPTION, PAYMENT_HISTORY } from "@/lib/dashboard/subscription";
import { toast } from "@/lib/toast";

export default function SubscriptionSettings() {
  const [configuratorOpen, setConfiguratorOpen] = useState(false);
  const currentPlan = TARIFF_PLANS.find((p) => p.id === CURRENT_SUBSCRIPTION.planId)!;

  const payMock = () => toast("Переход к оплате…");

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h1 className="text-xl font-bold text-ink sm:text-2xl">Подписка</h1>
        <p className="mt-0.5 text-sm text-ink-muted">Текущий тариф, смена плана и история платежей</p>
      </div>

      <SettingsCard title="Текущий тариф">
        <div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
          <div>
            <p className="text-lg font-bold text-ink">
              {currentPlan.name} — {currentPlan.price.toLocaleString("ru-RU")} ₽/мес
            </p>
            <p className="text-sm text-ink-muted">Продление {CURRENT_SUBSCRIPTION.renewsOn}</p>
          </div>
          <span className="inline-flex w-fit items-center gap-1.5 rounded-full bg-success/12 px-3 py-1 text-xs font-medium text-success">
            <Icon name="check-bold" size={13} aria-hidden="true" /> Активна
          </span>
        </div>
        <ul className="mt-4 grid grid-cols-1 gap-2 sm:grid-cols-2">
          {currentPlan.features.map((f) => (
            <li key={f} className="flex items-start gap-2 text-sm text-ink">
              <Icon name="check" size={15} className="mt-0.5 shrink-0 text-brand" aria-hidden="true" />
              {f}
            </li>
          ))}
        </ul>
      </SettingsCard>

      <SettingsCard title="Сменить тариф">
        <div className="grid grid-cols-1 gap-4 sm:grid-cols-3">
          {TARIFF_PLANS.map((p) => {
            const isCurrent = p.id === CURRENT_SUBSCRIPTION.planId;
            return (
              <div
                key={p.id}
                className={`rounded-2xl border p-5 ${isCurrent ? "border-brand/40 bg-brand/5" : "border-border bg-surface-soft"}`}
              >
                <p className="text-base font-bold text-ink">{p.name}</p>
                <p className="mt-1 text-2xl font-bold text-ink">
                  {p.price.toLocaleString("ru-RU")} ₽<span className="text-sm font-normal text-ink-muted">/мес</span>
                </p>
                <p className="mt-1 text-xs text-ink-muted">{p.tagline}</p>
                <button
                  type="button"
                  onClick={payMock}
                  disabled={isCurrent}
                  className="btn-glass-blue mt-4 inline-flex w-full items-center justify-center px-4 py-2.5 text-sm font-semibold disabled:opacity-50"
                >
                  {isCurrent ? "Текущий тариф" : "Оплатить через ЮKassa"}
                </button>
              </div>
            );
          })}

          <div className="rounded-2xl border border-border bg-surface-soft p-5">
            <p className="text-base font-bold text-ink">Свой тариф</p>
            <p className="mt-1 text-sm text-ink-muted">Гибкий конструктор под ваши объёмы</p>
            <button
              type="button"
              onClick={() => setConfiguratorOpen(true)}
              className="btn-glass mt-4 inline-flex w-full items-center justify-center px-4 py-2.5 text-sm font-semibold"
            >
              Настроить
            </button>
          </div>
        </div>
      </SettingsCard>

      <SettingsCard title="История платежей">
        <ul className="flex flex-col gap-2">
          {PAYMENT_HISTORY.map((r) => (
            <li key={r.id} className="flex items-center gap-3 rounded-2xl border border-border bg-surface-soft px-4 py-3">
              <Icon name="receipt" size={18} className="shrink-0 text-ink-muted" aria-hidden="true" />
              <div className="min-w-0 flex-1">
                <p className="truncate text-sm font-medium text-ink">
                  {r.plan} · {r.amount.toLocaleString("ru-RU")} ₽
                </p>
                <p className="truncate text-xs text-ink-muted">{r.date}</p>
              </div>
              <span className={`shrink-0 text-xs font-medium ${r.status === "Оплачено" ? "text-success" : "text-red-500"}`}>
                {r.status}
              </span>
            </li>
          ))}
        </ul>
        <p className="mt-3 text-xs text-ink-muted">
          Оплата обрабатывается сервисом ЮKassa — данные банковской карты не хранятся и не
          передаются UCust.
        </p>
      </SettingsCard>

      <TariffConfigurator
        open={configuratorOpen}
        onClose={() => setConfiguratorOpen(false)}
        onStart={() => {
          setConfiguratorOpen(false);
          payMock();
        }}
      />
    </div>
  );
}
```

- [ ] **Step 2: Create the page route**

Create `app/dashboard/subscription/page.tsx`:

```tsx
import SubscriptionSettings from "@/components/dashboard/subscription/SubscriptionSettings";

export default function Page() {
  return <SubscriptionSettings />;
}
```

- [ ] **Step 3: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Manual verification**

With `npm run dev` running, navigate to `http://localhost:3000/dashboard/subscription`. Verify:
- "Текущий тариф" shows "Бизнес — 3 500 ₽/мес", renewal date "11 августа 2026", "Активна" badge,
  and the Бизнес feature list.
- "Сменить тариф" shows 3 cards: Старт (enabled "Оплатить через ЮKassa" button), Бизнес (disabled
  button labeled "Текущий тариф"), Свой тариф ("Настроить" button).
- Clicking "Оплатить через ЮKassa" on Старт shows a toast "Переход к оплате…".
- Clicking "Настроить" on Свой тариф opens the existing `TariffConfigurator` modal; interacting
  with its sliders/addons works as it already does on the marketing pricing page; clicking its
  start/confirm action closes the modal and shows the same "Переход к оплате…" toast.
- "История платежей" lists the 3 mock rows with dates, amounts, and "Оплачено" status.

- [ ] **Step 5: Commit**

```bash
cd C:\Claude\UCust
git add components/dashboard/subscription app/dashboard/subscription
git commit -m "feat(subscription): add subscription management page"
```

---

### Task 8: Support mock data

**Files:**
- Create: `lib/dashboard/support.ts`

**Interfaces:**
- Produces: `SUPPORT_FAQ: FaqItem[]`, `SUPPORT_TICKETS: SupportTicket[]`, `SUPPORT_CONTACTS`,
  and the `SupportTicket` type. Consumed by Task 9 (`SupportSettings.tsx`).

- [ ] **Step 1: Create the file**

```ts
export interface FaqItem {
  q: string;
  a: string;
}

export const SUPPORT_FAQ: FaqItem[] = [
  {
    q: "Как сменить тариф?",
    a: "Перейдите в раздел «Подписка» в меню профиля и выберите новый тариф — оплата пройдёт через ЮKassa.",
  },
  {
    q: "Можно ли подключить несколько соцсетей?",
    a: "Да, все тарифы поддерживают подключение всех доступных соцсетей и мессенджеров без ограничений по количеству.",
  },
  {
    q: "Что будет с данными при отмене подписки?",
    a: "Данные сохраняются 30 дней после отмены — этого достаточно, чтобы вернуться без потерь.",
  },
  {
    q: "Как отменить подписку?",
    a: "Напишите в поддержку через форму ниже или на support@ucust.online — отменим и не спишем следующий платёж.",
  },
  {
    q: "Поддерживаете ли вы юрлица?",
    a: "Да, для юрлиц и ИП доступны закрывающие документы — запросите их через форму обращения.",
  },
];

export interface SupportTicket {
  id: string;
  subject: string;
  message: string;
  date: string;
  status: "Открыт" | "Закрыт";
}

export const SUPPORT_TICKETS: SupportTicket[] = [
  {
    id: "t1",
    subject: "Не публикуется пост в VK",
    message: "Пост завис в статусе «публикация» больше часа.",
    date: "2 дня назад",
    status: "Закрыт",
  },
  {
    id: "t2",
    subject: "Вопрос по тарифу Бизнес",
    message: "Можно ли добавить четвёртый бизнес-профиль?",
    date: "5 дней назад",
    status: "Закрыт",
  },
];

export const SUPPORT_CONTACTS = {
  email: "support@ucust.online",
  telegram: "@ucust_support",
  phone: "+7 900 123-45-67",
};
```

- [ ] **Step 2: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
cd C:\Claude\UCust
git add lib/dashboard/support.ts
git commit -m "feat(support): add mock FAQ/ticket/contact data"
```

---

### Task 9: Support page

**Files:**
- Create: `components/dashboard/support/SupportSettings.tsx`
- Create: `app/dashboard/support/page.tsx`

**Interfaces:**
- Consumes: `SUPPORT_FAQ`, `SUPPORT_TICKETS`, `SUPPORT_CONTACTS`, `SupportTicket` (Task 8),
  `SettingsCard`, `SelectField`, `TextArea` (existing `components/dashboard/settings/primitives.tsx`),
  `toast` (existing), `phone` icon (Task 1).
- Produces: nothing consumed by later tasks.

- [ ] **Step 1: Create `SupportSettings.tsx`**

```tsx
"use client";

import { useState } from "react";
import Icon from "@/components/ui/Icon";
import { SettingsCard, SelectField, TextArea } from "@/components/dashboard/settings/primitives";
import { SUPPORT_FAQ, SUPPORT_TICKETS, SUPPORT_CONTACTS, type SupportTicket } from "@/lib/dashboard/support";
import { toast } from "@/lib/toast";

const SUBJECTS = ["Технический вопрос", "Вопрос по оплате", "Другое"];

function FaqRow({ q, a }: { q: string; a: string }) {
  const [open, setOpen] = useState(false);
  return (
    <div className="border-b border-border/40 py-3 last:border-0">
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        aria-expanded={open}
        className="flex w-full items-center justify-between gap-2 text-left text-sm font-medium text-ink"
      >
        {q}
        <Icon
          name="chevron-down"
          size={15}
          className={`shrink-0 text-ink-muted transition-transform ${open ? "rotate-180" : ""}`}
          aria-hidden="true"
        />
      </button>
      {open && <p className="mt-2 text-sm text-ink-muted">{a}</p>}
    </div>
  );
}

export default function SupportSettings() {
  const [subject, setSubject] = useState(SUBJECTS[0]);
  const [message, setMessage] = useState("");
  const [tickets, setTickets] = useState<SupportTicket[]>(SUPPORT_TICKETS);

  const submit = () => {
    if (!message.trim()) return;
    const next: SupportTicket = {
      id: `t-${tickets.length + 1}-${subject}`,
      subject,
      message,
      date: "только что",
      status: "Открыт",
    };
    setTickets((t) => [next, ...t]);
    setMessage("");
    toast("Обращение отправлено");
  };

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h1 className="text-xl font-bold text-ink sm:text-2xl">Поддержка</h1>
        <p className="mt-0.5 text-sm text-ink-muted">Контакты, обращения и частые вопросы</p>
      </div>

      <SettingsCard title="Контакты">
        <div className="grid grid-cols-1 gap-3 sm:grid-cols-3">
          <a
            href={`mailto:${SUPPORT_CONTACTS.email}`}
            className="flex items-center gap-3 rounded-2xl border border-border bg-surface-soft px-4 py-3 transition hover:border-brand/30"
          >
            <Icon name="mail" size={18} className="shrink-0 text-ink-muted" aria-hidden="true" />
            <span className="truncate text-sm text-ink">{SUPPORT_CONTACTS.email}</span>
          </a>
          <a
            href={`https://t.me/${SUPPORT_CONTACTS.telegram.replace("@", "")}`}
            target="_blank"
            rel="noreferrer"
            className="flex items-center gap-3 rounded-2xl border border-border bg-surface-soft px-4 py-3 transition hover:border-brand/30"
          >
            <Icon name="send" size={18} className="shrink-0 text-ink-muted" aria-hidden="true" />
            <span className="truncate text-sm text-ink">{SUPPORT_CONTACTS.telegram}</span>
          </a>
          <a
            href={`tel:${SUPPORT_CONTACTS.phone.replace(/\s/g, "")}`}
            className="flex items-center gap-3 rounded-2xl border border-border bg-surface-soft px-4 py-3 transition hover:border-brand/30"
          >
            <Icon name="phone" size={18} className="shrink-0 text-ink-muted" aria-hidden="true" />
            <span className="truncate text-sm text-ink">{SUPPORT_CONTACTS.phone}</span>
          </a>
        </div>
      </SettingsCard>

      <SettingsCard title="Написать в поддержку">
        <div className="flex flex-col gap-4">
          <SelectField label="Тема" value={subject} onChange={setSubject} options={SUBJECTS} />
          <TextArea label="Сообщение" value={message} onChange={setMessage} placeholder="Опишите проблему или вопрос" />
          <button
            type="button"
            onClick={submit}
            className="btn-glass-blue inline-flex w-fit items-center justify-center px-5 py-2.5 text-sm font-semibold"
          >
            Отправить обращение
          </button>
        </div>
      </SettingsCard>

      <SettingsCard title="Частые вопросы">
        {SUPPORT_FAQ.map((item) => (
          <FaqRow key={item.q} q={item.q} a={item.a} />
        ))}
      </SettingsCard>

      <SettingsCard title="История обращений">
        <ul className="flex flex-col gap-2">
          {tickets.map((t) => (
            <li key={t.id} className="flex items-center gap-3 rounded-2xl border border-border bg-surface-soft px-4 py-3">
              <Icon name="message" size={18} className="shrink-0 text-ink-muted" aria-hidden="true" />
              <div className="min-w-0 flex-1">
                <p className="truncate text-sm font-medium text-ink">{t.subject}</p>
                <p className="truncate text-xs text-ink-muted">{t.date}</p>
              </div>
              <span className={`shrink-0 text-xs font-medium ${t.status === "Открыт" ? "text-brand" : "text-ink-muted"}`}>
                {t.status}
              </span>
            </li>
          ))}
        </ul>
      </SettingsCard>
    </div>
  );
}
```

- [ ] **Step 2: Create the page route**

Create `app/dashboard/support/page.tsx`:

```tsx
import SupportSettings from "@/components/dashboard/support/SupportSettings";

export default function Page() {
  return <SupportSettings />;
}
```

- [ ] **Step 3: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Manual verification**

With `npm run dev` running, navigate to `http://localhost:3000/dashboard/support`. Verify:
- "Контакты" shows 3 clickable rows (email/Telegram/phone) with correct `mailto:`/`https://t.me/`/`tel:` links.
- "Написать в поддержку": pick a subject, type a message, click "Отправить обращение" — a toast
  "Обращение отправлено" appears, the message field clears, and a new row with status "Открыт"
  and date "только что" appears at the top of "История обращений" below.
- Clicking "Отправить обращение" with an empty message does nothing (no empty ticket added).
- "Частые вопросы" shows 5 collapsed rows; clicking one expands its answer and rotates the chevron;
  clicking again collapses it.
- "История обращений" initially shows the 2 mock tickets (both "Закрыт") plus any submitted above.

- [ ] **Step 5: Commit**

```bash
cd C:\Claude\UCust
git add components/dashboard/support app/dashboard/support
git commit -m "feat(support): add support page"
```

---

### Task 10: Update `ProfileMenu` — rename + add 3 new links

**Files:**
- Modify: `components/dashboard/ProfileMenu.tsx:77-83` (the "Настройки" link block)

**Interfaces:**
- Consumes: routes from Tasks 5, 7, 9 (`/dashboard/appearance`, `/dashboard/subscription`,
  `/dashboard/support`), `card` icon (Task 1).
- Produces: nothing consumed by later tasks (final task before verification).

- [ ] **Step 1: Replace the "Настройки" link block**

In `components/dashboard/ProfileMenu.tsx`, replace:

```tsx
          <Link
            href="/dashboard/account"
            onClick={() => setOpen(false)}
            className="flex w-full items-center gap-2.5 rounded-xl px-3 py-2 text-left text-sm text-ink transition-colors hover:bg-surface-soft"
          >
            <Icon name="settings" size={16} aria-hidden="true" /> Настройки
          </Link>
          <div className="my-1 h-px bg-border" />
```

with:

```tsx
          <Link
            href="/dashboard/account"
            onClick={() => setOpen(false)}
            className="flex w-full items-center gap-2.5 rounded-xl px-3 py-2 text-left text-sm text-ink transition-colors hover:bg-surface-soft"
          >
            <Icon name="settings" size={16} aria-hidden="true" /> Управление аккаунтом
          </Link>
          <Link
            href="/dashboard/appearance"
            onClick={() => setOpen(false)}
            className="flex w-full items-center gap-2.5 rounded-xl px-3 py-2 text-left text-sm text-ink transition-colors hover:bg-surface-soft"
          >
            <Icon name="image" size={16} aria-hidden="true" /> Оформление
          </Link>
          <Link
            href="/dashboard/subscription"
            onClick={() => setOpen(false)}
            className="flex w-full items-center gap-2.5 rounded-xl px-3 py-2 text-left text-sm text-ink transition-colors hover:bg-surface-soft"
          >
            <Icon name="card" size={16} aria-hidden="true" /> Подписка
          </Link>
          <Link
            href="/dashboard/support"
            onClick={() => setOpen(false)}
            className="flex w-full items-center gap-2.5 rounded-xl px-3 py-2 text-left text-sm text-ink transition-colors hover:bg-surface-soft"
          >
            <Icon name="help" size={16} aria-hidden="true" /> Поддержка
          </Link>
          <div className="my-1 h-px bg-border" />
```

- [ ] **Step 2: Typecheck**

Run: `cd C:\Claude\UCust && npx tsc --noEmit`
Expected: no errors.

- [ ] **Step 3: Manual verification**

With `npm run dev` running, open `http://localhost:3000/dashboard`, click the profile avatar
(top-right). Verify the dropdown shows, in order: profile name/email, (mobile-only theme
toggle), "Управление аккаунтом", "Оформление", "Подписка", "Поддержка", divider, (mobile-only
"Правовое"), "Выйти". Click each of the 3 new links and confirm they navigate to the correct
page and close the dropdown.

- [ ] **Step 4: Commit**

```bash
cd C:\Claude\UCust
git add components/dashboard/ProfileMenu.tsx
git commit -m "feat(profile-menu): rename Настройки to Управление аккаунтом, add Оформление/Подписка/Поддержка links"
```

---

### Task 11: Full integration pass (both themes, both breakpoints)

**Files:** none (verification only).

- [ ] **Step 1: Desktop, light theme**

With `npm run dev` running, resize the browser to 1440×900, switch to light theme. Walk through:
profile menu → Управление аккаунтом → Оформление (pick a preset, confirm it applies) →
Подписка → Поддержка → Выйти (confirm it navigates to `/`). Confirm no layout breakage, no
console errors (check via `mcp__plugin_playwright_playwright__browser_console_messages` or the
browser devtools console).

- [ ] **Step 2: Desktop, dark theme**

Toggle to dark theme, repeat the same walkthrough. Pay special attention to the appearance page's
scrim (background should stay legible) and to the subscription/support cards' contrast.

- [ ] **Step 3: Mobile (390×844), both themes**

Resize to 390×844. Open the profile menu from the top bar — confirm the theme toggle and
"Правовое" section are visible here (they're `sm:hidden`/`lg:hidden` respectively), and the 3 new
links work. Visit all 3 new pages and confirm they scroll correctly and nothing overflows
horizontally.

- [ ] **Step 4: Final typecheck and build**

Run: `cd C:\Claude\UCust && npx tsc --noEmit && npm run build`
Expected: both succeed with no errors (the build step also catches any Next.js-specific issues
the dev server tolerates, like missing `Image` `sizes`/`loader` misconfigurations).

- [ ] **Step 5: Final commit (only if Step 4 required fixes)**

If Step 4 required any fix, commit it:

```bash
cd C:\Claude\UCust
git add -A
git commit -m "fix: resolve build issues from profile menu account pages"
```

If no fixes were needed, this task requires no commit — the feature is complete as of Task 10's
commit.
