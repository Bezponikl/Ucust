spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/business_service_db
    username: ETA_DBUser
    password: ${DB_PASSWORD}
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: service-user
    password: servicepassword
    virtual-host: universal-host

minio:
  endpoint: http://minio:9000
  default:
    path: https://api.ucust.n4d3sh1k4.site/s3/business-service/
