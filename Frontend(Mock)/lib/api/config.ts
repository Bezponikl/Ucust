/**
 * Базовый адрес API вместе с префиксом версии контракта (`/api/v0`).
 * В контуре фронт и бэк живут за одним nginx, поэтому по умолчанию путь
 * относительный — браузер сам подставит текущий origin. Для локальной
 * разработки (фронт на :3000, шлюз на :8100 или тестовый контур) адрес
 * задаётся в .env.local, варианты перечислены в .env.example.
 */
export const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? "/api/v0";
