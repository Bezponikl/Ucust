services:
    redis:
        image: redis:latest
        container_name: redis-reshala
        ports:
            - '6379:6379'
        volumes:
            - redis-data:/data
        command: redis-server --requirepass redispass --appendonly yes
        restart: unless-stopped

volumes:
    redis-data:
        driver: local
